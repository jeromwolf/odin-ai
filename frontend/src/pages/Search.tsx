import React from 'react';
import { Box, Typography } from '@mui/material';

const Search: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">입찰 검색</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        검색 페이지는 현재 개발 중입니다.
      </Typography>
    </Box>
  );
};

export default Search;
