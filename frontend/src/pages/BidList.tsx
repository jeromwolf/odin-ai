import React from 'react';
import { Box, Typography } from '@mui/material';

const BidList: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">입찰 목록</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        입찰 목록 페이지는 현재 개발 중입니다.
      </Typography>
    </Box>
  );
};

export default BidList;